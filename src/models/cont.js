export default class Conts {
  constructor(id, name, email, subject, firstName, complaint) {
    this.id = id;
    this.name = name;
    this.email = email;
    this.subject = subject;
    this.firstName = firstName;
    this.complaint = complaint;
  }
}
